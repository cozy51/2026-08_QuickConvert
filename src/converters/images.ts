import { jsPDF } from 'jspdf';
export const readDataUrl=(f:File)=>new Promise<string>((ok,no)=>{const r=new FileReader();r.onload=()=>ok(r.result as string);r.onerror=no;r.readAsDataURL(f)});
const loadImage=(src:string)=>new Promise<HTMLImageElement>((ok,no)=>{const i=new Image();i.onload=()=>ok(i);i.onerror=no;i.src=src});
export async function imagesToPdf(files:File[]){let pdf:jsPDF|undefined; for(const file of files){const data=await readDataUrl(file);const img=await loadImage(data);const landscape=img.width>img.height;const w=landscape?297:210,h=landscape?210:297; if(!pdf)pdf=new jsPDF({orientation:landscape?'landscape':'portrait',unit:'mm',format:'a4'});else pdf.addPage('a4',landscape?'landscape':'portrait');const scale=Math.min(w/img.width,h/img.height);const iw=img.width*scale,ih=img.height*scale;pdf.addImage(data,file.type.includes('png')?'PNG':'JPEG',(w-iw)/2,(h-ih)/2,iw,ih)}return pdf!.output('blob')}
export async function convertImage(file:File,format:'png'|'jpeg'|'webp',quality=.9){const img=await loadImage(await readDataUrl(file));const c=document.createElement('canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;const ctx=c.getContext('2d')!;if(format==='jpeg'){ctx.fillStyle='#fff';ctx.fillRect(0,0,c.width,c.height)}ctx.drawImage(img,0,0);return new Promise<Blob>((ok,no)=>c.toBlob(b=>b?ok(b):no(new Error('画像を変換できませんでした')),`image/${format}`,quality))}

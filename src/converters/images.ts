import { jsPDF } from 'jspdf';
export const readDataUrl=(f:File)=>new Promise<string>((ok,no)=>{const r=new FileReader();r.onload=()=>ok(r.result as string);r.onerror=no;r.readAsDataURL(f)});
const loadImage=(src:string)=>new Promise<HTMLImageElement>((ok,no)=>{const i=new Image();i.onload=()=>ok(i);i.onerror=()=>no(new Error('このブラウザでは画像を読み込めません。HEICの場合はSafariなどHEIC対応ブラウザをお試しください。'));i.src=src});
const drawImage=async(file:File,background=false)=>{const img=await loadImage(await readDataUrl(file));const c=document.createElement('canvas');c.width=img.naturalWidth||img.width;c.height=img.naturalHeight||img.height;if(!c.width||!c.height)throw new Error('画像のサイズを取得できませんでした');const ctx=c.getContext('2d')!;if(background){ctx.fillStyle='#fff';ctx.fillRect(0,0,c.width,c.height)}ctx.drawImage(img,0,0,c.width,c.height);return c};
const canvasBlob=(canvas:HTMLCanvasElement,type:string,quality?:number)=>new Promise<Blob>((ok,no)=>canvas.toBlob(b=>b?ok(b):no(new Error('画像を変換できませんでした')),type,quality));
export async function imagesToPdf(files:File[]){let pdf:jsPDF|undefined; for(const file of files){const canvas=await drawImage(file,true);const landscape=canvas.width>canvas.height;const w=landscape?297:210,h=landscape?210:297;if(!pdf)pdf=new jsPDF({orientation:landscape?'landscape':'portrait',unit:'mm',format:'a4'});else pdf.addPage('a4',landscape?'landscape':'portrait');const scale=Math.min(w/canvas.width,h/canvas.height);const iw=canvas.width*scale,ih=canvas.height*scale;pdf.addImage(canvas.toDataURL('image/jpeg',.92),'JPEG',(w-iw)/2,(h-ih)/2,iw,ih)}return pdf!.output('blob')}
export async function convertImage(file:File,format:'png'|'jpeg'|'webp',quality=.9){return canvasBlob(await drawImage(file,format==='jpeg'),`image/${format}`,quality)}
